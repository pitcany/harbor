# This file can be used for additional environment variables
# specifically for the 'mcpo' service.
# You can also use the "harbor env" command to set these variables.

# Trust /workspace/AI for the git MCP server (container uid != host uid).
GIT_CONFIG_COUNT=1
GIT_CONFIG_KEY_0=safe.directory
GIT_CONFIG_VALUE_0=*
